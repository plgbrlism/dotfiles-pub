# kitty terminal emulator colors
# generated by rizzoo — adaptive to light/dark mode

foreground         {{ mode == dark ? on_surface_dark : on_surface_light }}
background         {{ mode == dark ? background_dark : background_light }}
cursor             {{ mode == dark ? primary_dark : primary_light }}

active_tab_foreground     {{ mode == dark ? on_primary_dark : on_primary_light }}
active_tab_background     {{ mode == dark ? primary_dark : primary_light }}
inactive_tab_foreground   {{ mode == dark ? outline_dark : outline_light }}
inactive_tab_background   {{ mode == dark ? surface_container_dark : surface_container_light }}

active_border_color   {{ mode == dark ? primary_dark : primary_light }}
inactive_border_color {{ mode == dark ? surface_container_dark : surface_container_light }}
bell_border_color     {{ mode == dark ? error_dark : error_light }}

color0  {{ mode == dark ? surface_dark : inverse_surface_light }}
color1  {{ mode == dark ? error_dark : error_light }}
color2  {{ mode == dark ? on_tertiary_container_dark : on_tertiary_fixed_variant_light }}
color3  {{ mode == dark ? tertiary_dark : tertiary_light }}
color4  {{ mode == dark ? on_primary_container_dark : primary_container_light }}
color5  {{ mode == dark ? secondary_dark : secondary_light }}
color6  {{ mode == dark ? outline_dark : outline_light }}
color7  {{ mode == dark ? on_surface_dark : on_surface_light }}
color8  {{ mode == dark ? outline_variant_dark : surface_tint_light }}
color9  {{ mode == dark ? error_container_dark : error_container_light }}
color10 {{ mode == dark ? tertiary_fixed_dim_dark : tertiary_container_light }}
color11 {{ mode == dark ? tertiary_fixed_dark : on_secondary_fixed_variant_light }}
color12 {{ mode == dark ? primary_dark : primary_light }}
color13 {{ mode == dark ? secondary_fixed_dim_dark : secondary_container_light }}
color14 {{ mode == dark ? on_surface_variant_dark : on_surface_variant_light }}
color15 {{ mode == dark ? inverse_surface_dark : inverse_surface_light }}
