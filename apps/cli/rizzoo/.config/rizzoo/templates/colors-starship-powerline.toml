"$schema" = 'https://starship.rs/config-schema.json'

format = """
[]({{ primary }})\
$os\
$username\
[](bg:{{ secondary }} fg:{{ primary }})\
$directory\
[](fg:{{ secondary }} bg:{{ tertiary }})\
$git_branch\
$git_status\
[](fg:{{ tertiary }} bg:{{ primary_container }})\
$c\
$cpp\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$maven\
$nodejs\
$bun\
$nim\
$rust\
$scala\
[](fg:{{ primary_container }} bg:{{ secondary_container }})\
$docker_context\
[](fg:{{ secondary_container }} bg:{{ tertiary_container }})\
$time\
[ ](fg:{{ tertiary_container }})\
"""

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bg:{{ primary }} fg:{{ on_primary }}"
style_root = "bg:{{ primary }} fg:{{ on_primary }}"
format = '[$user ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bg:{{ primary }} fg:{{ on_primary }}"
disabled = true # Disabled by default

[directory]
style = "bg:{{ secondary }} fg:{{ on_secondary }}"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[c]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[cpp]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:{{ secondary_container }} fg:{{ on_secondary_container }}"
format = '[ $symbol $context ]($style)'

[elixir]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:{{ tertiary }} fg:{{ on_tertiary }}"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:{{ tertiary }} fg:{{ on_tertiary }}"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[gradle]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[maven]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[bun]
symbol = ""
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:{{ primary_container }} fg:{{ on_primary_container }}"
format = '[ $symbol ($version) ]($style)'

[time]
disabled = false
time_format = "%R" # Hour:Minute Format
style = "bg:{{ tertiary_container }} fg:{{ on_tertiary_container }}"
format = '[ ♥ $time ]($style)'
